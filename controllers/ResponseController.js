const Response = require("../models/Response");

const ResponseController = {
// Endpoint create: post response from comment, optional etiquetar
// Endpoint like: dar like a response
// Endpoint unlike: quitar like a repsonse
// Delete response: Usuario solo puede borrar if isAuthor, pero is es admin, puede borrar todo
}

module.exports = ResponseController;